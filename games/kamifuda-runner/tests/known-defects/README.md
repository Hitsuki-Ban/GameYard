# Known presentation defect

`844x390-clipped-primary-action.png` is a 1× CSS-pixel capture of the untouched v4 title at the issue #48 short-landscape reference viewport. The title's primary mode controls extend outside the visible action area, so it is retained as failing evidence for #55 rather than asserted as a visual golden.
