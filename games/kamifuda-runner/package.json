{
  "name": "@gameyard/kamifuda-runner",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "browser:install": "vp exec playwright install chromium",
    "dev": "vp dev",
    "check": "vp check && vp exec node tools/verify-source-snapshot.mjs",
    "test": "vp run check && vp exec playwright test --config playwright.baseline.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
