{
  "name": "@gameyard/tumbledrum",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "browser:install": "vp exec uv run playwright install chromium",
    "dev": "vp dev --host 127.0.0.1 --port 5175 --strictPort",
    "build": "vp exec uv run python tools/build_single.py",
    "check": "vp check",
    "test": "vp exec uv run python tests/gameyard_baseline.py"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  }
}
