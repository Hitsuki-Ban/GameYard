{
  "name": "@gameyard/tumbledrum",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "browser:install": "vp exec uv run playwright install chromium",
    "dev": "vp dev",
    "build": "vp build",
    "check": "vp check",
    "test": "vp run build && vp exec uv run python tests/gameyard_baseline.py"
  },
  "dependencies": {
    "@gameyard/game-contract": "workspace:*",
    "@gameyard/guest-bridge": "workspace:*",
    "@gameyard/manifest-tools": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
