{
  "name": "@gameyard/pulse-link-overdrive",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev --host 127.0.0.1 --port 5174 --strictPort",
    "build": "vp build",
    "check": "vp check",
    "test": "vp exec playwright test --config playwright.baseline.config.ts"
  },
  "dependencies": {
    "@gameyard/game-contract": "workspace:*",
    "@gameyard/guest-bridge": "workspace:*",
    "@gameyard/manifest-tools": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
