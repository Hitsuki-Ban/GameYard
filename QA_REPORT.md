# QA 报告

## v3.4.0

构建：CROWN//BREAKER v3.4.0 规则扩展
日期：2026-07-21

### 本地结果

- `pnpm check`：通过；JavaScript 语法、必需文件、三语目录、Manifest 与 Service Worker 预缓存声明一致。
- `pnpm check:traits -- --targeted` 与 `pnpm check:traits --targeted`：均通过；覆盖 15 组定向行为契约，包括 3 个新增特质及既有 12 个特质的关键边界。
- `pnpm check:traits`：238.608 秒通过；以 6 个隔离浏览器上下文完成 15 个特质 × 100 个种子的 1,500 个组合，每个组合连续执行前三次玩家行动并等待完整结算。无无解、页面异常或超时。
- 三语呈现：新增 `重力 / Gravity / 重力`、`附身 / Possession / 憑依`、`连锁 / Lockstep / 連鎖` 均在中文、日文、英文的 HUD、路线卡与战利品界面完成检查。
- QA 边界：测试配置采用完整、严格的原子输入；未知特质、战利品、棋子、充能与不稳定阶段均明确拒绝。普通 URL 下 `window.__CB_TEST__` 为 `undefined`。
- 单文件版：`dist/crown-breaker-v3.4.0.html` 构建通过，大小 271,284 字节；不含外部脚本、样式、远程 URL 或 Service Worker。SHA-256：`8E50A707DF3118158AC93ADFDAE862588BF91B0D253BF8B8C8B85A584E15B27A`。
- 可视状态：黑方被连锁锁定时，Canvas 使用降亮与横线标记；独立截图复核通过，页面无控制台、脚本或资源请求错误。

### 平衡对照

- 命令：`pnpm sim -- --runs 100 --policy greedy --seed-base 1000`；177.3 秒完成，无 QA、模拟器或超时错误。
- v3.4.0：79 胜 / 21 负，平均深度 7.13，分数中位数 101,628.5；失败分布为王冠破碎 16、无合法行动 2、回合耗尽 3。
- 同种子 v3.3.0 基线：84 胜 / 16 负，平均深度 7.32，分数中位数 101,032.5。新增规则使胜率下降 5 个百分点、平均深度下降 0.19，分数中位数增加 596；未出现明显失控或流程性卡死。
- 新增特质的自然遭遇中，连锁为 38 / 41 胜，附身为 29 / 29 胜。重力只在棋组已有骑士时进入路线池，因此本批贪心策略没有自然遭遇；其敌方与战利品两侧仍由 100 种子强制烟测及定向行为契约覆盖。
- JSON 报告 SHA-256：`7095B59A78BABA6DFBDAA9167841DBABECBCD57F0E324EED7199B62A8B62D223`；Markdown 报告 SHA-256：`AC5A96771601714F136DF4675712969339B64D320F1F3F4F5F79F5172F429DC2`。

## v2.1.0

构建：CROWN//BREAKER v2.1.0
日期：2026-07-21

### 本地结果

- `pnpm check`：通过；覆盖 6 个 JavaScript 文件的语法检查，以及 11 个必需文件、3 份 Manifest、11 个预缓存声明的静态检查。
- `pnpm build:standalone dist/crown-breaker.html`：通过；生成单一 HTML，浏览器启动无错误或警告，不含外部脚本与样式表，图标以内联 data URL 提供。
- 翻译目录：`en`、`zh-CN`、`ja` 的键集合与插值占位符一致；缺键、缺参数、非法 locale 与非法语言偏好均会明确报错。
- 设备语言：按 `navigator.languages` 的顺序匹配中、日、英；无受支持语言时明确使用英文。覆盖混合语言列表、地区标签、空列表、无 `navigator` 与三种显式覆盖，共 8 项断言通过。
- 用户覆盖：标题页与设置页的语言控件保持同步，选择在刷新后保留；`system` 在测试设备上解析为 `zh-CN`。
- 确定性：固定种子 `424242` 的活动局在 `zh-CN`、`ja`、`en`、`system` 间切换后，`crownBreaker.run.v2` 的原始存储字符串、RNG 状态与对局阶段均保持不变。
- 动态界面：战斗 HUD、升变、奖励、契约、失败原因、练习结算与最终结算均逐语种检查。固定种子 `8888` 通过测试接口完成 6 场流程，覆盖 5 次奖励、2 次精英双奖励、5 次契约选择和日文最终结算。
- QA 边界：`?qa` 下提供测试接口；普通 URL 下 `window.__CB_TEST__` 为 `undefined`。
- 响应式：1440 × 900、820 × 1180、390 × 844、360 × 640 均无横向或页面纵向溢出；关键战斗控件保持在视口内，紧凑高度按设计隐藏次要棋组栏。
- PWA：本地 HTTP 作用域注册成功，缓存名为 `crown-breaker-v2.1.0`；首次联网加载后切为离线并刷新，页面、Canvas、三语脚本与 QA 模式均可恢复。额外创建的同源其他项目缓存会被保留，且不会被本应用跨缓存读取。

### 在线发布

- 公开仓库：`https://github.com/Hitsuki-Ban/CrownBreaker`，可见性为 Public，默认分支为 `main`。
- GitHub Pages：`https://hitsuki-ban.github.io/CrownBreaker/`，从 `main:/` 构建成功并强制 HTTPS；HTTP 请求返回 301 到 HTTPS。
- 线上资源：页面、CSS、i18n、游戏脚本、Service Worker、3 份 Manifest、SVG 与 192/512 PNG 均在 `/CrownBreaker/` 子路径返回 200，MIME 类型正确。
- 线上语言：设备默认解析为 `zh-CN`；切换英文后 `<html lang>`、标题与 Manifest 同步更新，再切回 `system` 后恢复中文。
- 线上 Service Worker：作用域为 `https://hitsuki-ban.github.io/CrownBreaker/`，脚本为该目录下的 `sw.js`，缓存名为 `crown-breaker-v2.1.0`。
- 线上离线：首次加载后断网刷新成功，页面、Canvas、活动局续接、语言偏好和 QA 页面均从缓存恢复；恢复联网后普通 URL 不暴露 `window.__CB_TEST__`。
- 浏览器控制台：线上加载、语言切换、活动局、离线刷新和普通 URL 均为 0 errors / 0 warnings。

## v2.0.0（历史记录）

构建：CROWN//BREAKER v2.0.0
日期：2026-07-21

以下结果来自 v2.0.0 制作阶段的历史记录。执行这些断言的自动化脚本未随发行包提供，因此当前仓库无法独立复跑或据此声明 v2.1.0 已通过。

## 结果

- 核心与响应式自动检查：34 / 34 通过。
- 存档与确定性自动检查：21 / 21 通过。
- JavaScript 语法检查：通过。
- 总计：55 / 55 自动断言通过。

## 核心流程

已覆盖：

- 新 Run 初始化、初始棋组与黑方行动预告。
- 三连手获得 3 次免费行动，且行动次数正确递减。
- 通过测试接口完成 6 场完整流程并进入结算页。
- 训练局中吃子、推进、打开升变页、4 个升变选项和升变结果。
- 战后 3 选 1 棋印页。
- 常局 / 险局路线页。
- 结算评级、统计显示和运行时异常监听。

## 存档与回滚

已覆盖：

- 对局中途续接：手数、棋子 ID、位置、存活、移动状态和黑方预告完全一致。
- 棋印选择页续接：候选项与随机状态不变。
- 路线选择页续接：合约内容与随机状态不变。
- 升变选择页续接：弹窗恢复，选择后正确写入跨局棋组。
- 失败后回滚：本局内升变撤销，棋组恢复。
- 重开本局：棋盘与棋组一致。
- 固定种子：相同种子生成相同开局敌阵和行动预告。

## 推荐行动冒烟测试

- 种子 100—116：推荐行动驱动的首局均完成。
- 种子 7001、7002：选择常规路线并持续执行推荐行动，均完成 6 局。

这项检查用于发现明显卡死、无合法行动和推荐器失效，不代表难度分布或长期平衡结论。

## 响应式

自动检查以下视口无横向溢出，Canvas 尺寸有效，且未捕获运行时异常：

- 1440 × 900
- 820 × 1180
- 390 × 844
- 360 × 640

人工截图检查覆盖标题、对局、升变、棋印、路线和结算状态，以及桌面、平板、手机与紧凑手机布局。

## 静态检查

- 无运行时第三方依赖。
- 无 CDN、外部图片、外部字体或外部音频请求。
- 可见文案中未包含开发提示、模型说明或提示词内容。
- 测试接口只在显式 QA 模式启用，正常运行时不挂载全局测试对象。
- Service Worker 只在非 `file:` 协议注册。
- 单文件版可直接从本地打开。

## 已知边界

- 棋盘落子需要鼠标或触控；键盘只提供三连手、提示和暂停快捷键。
- 浏览器会在首次用户输入前阻止音频播放，这是浏览器策略。
- PWA 安装与 Service Worker 需要 HTTP 或 HTTPS；直接打开单文件版时不注册离线缓存。
- 存档依赖 `localStorage`。隐私模式、浏览器清理或禁用站点存储会移除进度。
- 今日局按设备本地日期取种子，不是联网排行榜。
