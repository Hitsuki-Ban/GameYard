# QA 报告

## v2.1.0

构建：CROWN//BREAKER v2.1.0
日期：2026-07-21

### 本地结果

- `pnpm check`：通过；覆盖 6 个 JavaScript 文件的语法检查，以及 11 个必需文件、3 份 Manifest、11 个预缓存声明的静态检查。
- `pnpm build:standalone dist/crown-breaker.html`：通过；生成单一 HTML，浏览器启动无错误或警告，不含外部脚本与样式表，图标以内联 data URL 提供。
- 翻译目录：`en`、`zh-CN`、`ja` 的键集合与插值占位符一致；缺键、缺参数、非法 locale 与非法语言偏好均会明确报错。
- 设备语言：按 `navigator.languages` 的顺序匹配中、日、英；无受支持语言时明确使用英文。覆盖混合语言列表、地区标签、空列表、无 `navigator` 与三种显式覆盖，共 8 项断言通过。
- 用户覆盖：标题页与设置页的语言控件保持同步，选择在刷新后保留；`system` 在测试设备上解析为 `zh-CN`。
- 确定性：固定种子 `424242` 的活动局在 `zh-CN`、`ja`、`en`、`system` 间切换后，`crownBreaker.run.v2` 的原始存储字符串、RNG 状态与对局阶段均保持不变。
- 动态界面：战斗 HUD、升变、奖励、契约、失败原因、练习结算与最终结算均逐语种检查。固定种子 `8888` 通过测试接口完成 6 场流程，覆盖 5 次奖励、2 次精英双奖励、5 次契约选择和日文最终结算。
- QA 边界：`?qa` 下提供测试接口；普通 URL 下 `window.__CB_TEST__` 为 `undefined`。
- 响应式：1440 × 900、820 × 1180、390 × 844、360 × 640 均无横向或页面纵向溢出；关键战斗控件保持在视口内，紧凑高度按设计隐藏次要棋组栏。
- PWA：本地 HTTP 作用域注册成功，缓存名为 `crown-breaker-v2.1.0`；首次联网加载后切为离线并刷新，页面、Canvas、三语脚本与 QA 模式均可恢复。额外创建的同源其他项目缓存会被保留，且不会被本应用跨缓存读取。

### 在线发布

GitHub Pages 项目子路径与线上 Service Worker 作用域将在公开仓库发布后实测并补记；本地结果不代替线上部署验证。

## v2.0.0（历史记录）

构建：CROWN//BREAKER v2.0.0
日期：2026-07-21

以下结果来自 v2.0.0 制作阶段的历史记录。执行这些断言的自动化脚本未随发行包提供，因此当前仓库无法独立复跑或据此声明 v2.1.0 已通过。

## 结果

- 核心与响应式自动检查：34 / 34 通过。
- 存档与确定性自动检查：21 / 21 通过。
- JavaScript 语法检查：通过。
- 总计：55 / 55 自动断言通过。

## 核心流程

已覆盖：

- 新 Run 初始化、初始棋组与黑方行动预告。
- 三连手获得 3 次免费行动，且行动次数正确递减。
- 通过测试接口完成 6 场完整流程并进入结算页。
- 训练局中吃子、推进、打开升变页、4 个升变选项和升变结果。
- 战后 3 选 1 棋印页。
- 常局 / 险局路线页。
- 结算评级、统计显示和运行时异常监听。

## 存档与回滚

已覆盖：

- 对局中途续接：手数、棋子 ID、位置、存活、移动状态和黑方预告完全一致。
- 棋印选择页续接：候选项与随机状态不变。
- 路线选择页续接：合约内容与随机状态不变。
- 升变选择页续接：弹窗恢复，选择后正确写入跨局棋组。
- 失败后回滚：本局内升变撤销，棋组恢复。
- 重开本局：棋盘与棋组一致。
- 固定种子：相同种子生成相同开局敌阵和行动预告。

## 推荐行动冒烟测试

- 种子 100—116：推荐行动驱动的首局均完成。
- 种子 7001、7002：选择常规路线并持续执行推荐行动，均完成 6 局。

这项检查用于发现明显卡死、无合法行动和推荐器失效，不代表难度分布或长期平衡结论。

## 响应式

自动检查以下视口无横向溢出，Canvas 尺寸有效，且未捕获运行时异常：

- 1440 × 900
- 820 × 1180
- 390 × 844
- 360 × 640

人工截图检查覆盖标题、对局、升变、棋印、路线和结算状态，以及桌面、平板、手机与紧凑手机布局。

## 静态检查

- 无运行时第三方依赖。
- 无 CDN、外部图片、外部字体或外部音频请求。
- 可见文案中未包含开发提示、模型说明或提示词内容。
- 测试接口只在显式 QA 模式启用，正常运行时不挂载全局测试对象。
- Service Worker 只在非 `file:` 协议注册。
- 单文件版可直接从本地打开。

## 已知边界

- 棋盘落子需要鼠标或触控；键盘只提供三连手、提示和暂停快捷键。
- 浏览器会在首次用户输入前阻止音频播放，这是浏览器策略。
- PWA 安装与 Service Worker 需要 HTTP 或 HTTPS；直接打开单文件版时不注册离线缓存。
- 存档依赖 `localStorage`。隐私模式、浏览器清理或禁用站点存储会移除进度。
- 今日局按设备本地日期取种子，不是联网排行榜。
